/**
 * @param {number} input - A single array of positive integers, where each integer represents the maximum number of steps you can take forward from that hall
 * @returns {number} - Single integer representing the minimum number of steps needed to reach the exit. If it's impossible to reach the exit, return -1.
 */
export function getMinimumSteps(input: number[]): number {
  return -1;
}
